# Rust build output
target/

# C build output (includes clib4-src/build/ when rebuilding clib4 from source)
build/

# Fake linker logs
fake-linker.log

# Editor / IDE state
.vscode/
.idea/
*.swp
*.swo

# OS files
.DS_Store
Thumbs.db

# Scratch / one-off scripts. The canonical scratch directory for this
# project lives at C:/msys64/home/rich_/Projects/Rust/tmp (outside the
# repo). This pattern also keeps any in-repo tmp/ accidentally created
# from being tracked.
tmp/

# Tests/ orchestrator's sibling target dir (used to avoid Windows file
# locks when the runner spawns cargo test on itself).
Tests/target-sub/
