//! BattClockIFace interface vtable struct(s).
//!
//! Auto-generated by amigaos4-bindgen.

use crate::types::*;

// ---- BattClockIFace (battclock.h) ----

#[repr(C)]
pub struct BattClockIFace {
    pub data: InterfaceData,

    pub Obtain: unsafe extern "C" fn(*mut BattClockIFace) -> u32, // 0: 60
    pub Release: unsafe extern "C" fn(*mut BattClockIFace) -> u32, // 1: 64
    pub Expunge: unsafe extern "C" fn(*mut BattClockIFace), // 2: 68
    pub Clone: unsafe extern "C" fn(*mut BattClockIFace) -> *mut Interface, // 3: 72
    pub ResetBattClock: unsafe extern "C" fn(*mut BattClockIFace), // 4: 76
    pub ReadBattClock: unsafe extern "C" fn(*mut BattClockIFace) -> u32, // 5: 80
    pub WriteBattClock: unsafe extern "C" fn(*mut BattClockIFace, u32), // 6: 84
    pub ReadBattClockMem: unsafe extern "C" fn(*mut BattClockIFace, u32, u32) -> u32, // 7: 88
    pub WriteBattClockMem: unsafe extern "C" fn(*mut BattClockIFace, u32, u32, u32), // 8: 92
}

